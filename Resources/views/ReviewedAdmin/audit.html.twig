{#{% if admin.isGranted('EDIT', object) and admin.hasRoute('edit') %}#}

  <a href="{{ admin.generateObjectUrl('audit', object) }}" class="btn btn-sm btn-default edit_link" >
    <i class="glyphicon glyphicon-edit"></i>
    submit
  </a>
{#{% endif %}#}
